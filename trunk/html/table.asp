<%

/* table.asp - by Thomas Kjoernes <thomas@ipv.no> */

	if (!this.asplib) asplib = {};
	if (!this.asplib.html) asplib.html = {};

//--------------------------------------------------------------------
// asplib.html.Table()
//--------------------------------------------------------------------

asplib.html.Table = function() {
	this.__name = "asplib.html.Table";
};

asplib.html.Table.prototype.toString = function() {
};

%>