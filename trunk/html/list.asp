<%

/* list.asp - by Thomas Kjoernes <thomas@ipv.no> */

	if (!this.asplib) asplib = {};
	if (!this.asplib.html) asplib.html = {};

//--------------------------------------------------------------------
// asplib.html.List()
//--------------------------------------------------------------------

asplib.html.List = function() {
	this.__name = "asplib.html.List";
};

asplib.html.List.prototype.toString = function() {
};

%>